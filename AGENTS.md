# AGENTS.md

## 项目定位

- 本项目是用于秋招简历与技术面试展示的“水利工程运行数据智能分析 Agent”，主要技术栈为 Python、Streamlit、大语言模型 Tool Calling、RAG 和 Pandas。
- 目标是做一个规模适中、完整可运行、容易解释的 Demo，而不是商业级复杂系统。
- 优先完成可运行的 MVP，再逐步增加功能；不要在核心流程尚未跑通时提前引入复杂架构或进行大规模重构。
- 技术决策优先级依次为：正确性、可解释性、简单架构、代码可读性、Demo 展示效果。
- 多种方案均可行时，优先选择开发者能够在技术面试中清楚解释其工作原理的方案，并优先采用简单、稳定、主流的技术。

## 架构与实现约束

- 避免过度工程化；没有明确必要性时，不引入 Redis、Celery、Kubernetes、微服务等复杂技术。
- 除非现有方案存在明确问题，否则不要随意替换已经确定的核心技术栈或同时引入多个功能重叠的框架。
- LLM Provider、数据分析工具和 RAG 模块之间应保持清晰边界，避免业务逻辑与具体模型 API 强耦合，便于后续替换模型。
- 大模型只负责任务理解、工具选择和结果解释，不得直接完成数值计算。统计、趋势、相关性和异常检测必须由 Python 工具执行。
- Python 代码应保持模块化和易读；关键函数应提供 type hints，并在需要时编写简洁 docstring。
- API Key 和其他敏感配置必须通过环境变量提供，不得硬编码、写入示例数据或提交到版本库。

## 数据与安全边界

- 所有 Demo 工程数据必须明确标记为 `synthetic` 或 `simulated`，不得暗示或伪装成真实工程数据。
- 系统仅用于数据分析和技术演示，不得实现真实水闸、泵站或其他基础设施的控制接口。
- 大模型生成的工程解释、异常提示和知识库回答仅作为分析参考；不得将其表述为真实工程调度指令、安全结论或自动控制决策。
- 不得为了丰富简历而虚构技术指标、真实工程成果、数据来源或不存在的数据。

## 测试与文档

- 新增或修改核心功能时，应同步新增或更新对应的 pytest 测试。
- 测试或核心功能存在已知错误时，应优先定位并尝试修复，不得在存在已知核心错误的情况下直接宣告任务完成；确实无法解决时应明确说明原因。
- 每次完成主要修改后，应运行测试，并检查 Streamlit 应用能够正常启动；如无法完成，应明确记录原因。
- README 必须随项目功能、运行方式、配置或架构变化同步维护。
